diff --git a/app/static/app.js b/app/static/app.js
index cf9ae9e..b175bc7 100644
--- a/app/static/app.js
+++ b/app/static/app.js
@@ -847,7 +847,7 @@ $('#modal').addEventListener('click', (event) => {
 });
 
 document.addEventListener('keydown', (event) => {
-  if (event.key === 'Escape') closeModal();
+  if (event.key === 'Escape') { closeModal(); setMobileTabMenu(false); }
   if (event.key === 'Tab' && $('#modal').classList.contains('open')) {
     const focusable = $$('button:not(:disabled), a[href], input:not(:disabled), select:not(:disabled), textarea:not(:disabled), [tabindex="0"]', $('#modal'));
     if (!focusable.length) return;
@@ -871,9 +871,8 @@ function switchView(view, options = {}) {
     button.classList.toggle('active', active);
     button.setAttribute('aria-current', active ? 'page' : 'false');
   });
-  const mobileMoreViews = new Set(['preferences', 'blockers', 'skills', 'sources']);
-  $('#mobile-more')?.classList.toggle('active', mobileMoreViews.has(view));
-  setMobileNavSheet(false);
+  updateMobileTabDock(view);
+  setMobileTabMenu(false);
   $('#page-title').textContent = ({
     dashboard: 'לוח בקרה', jobs: 'משרות', applications: 'הגשות', blockers: 'דורש טיפול', skills: 'סקילים',
     sources: 'מקורות', preferences: 'העדפות חיפוש', profile: 'הפרופיל שלי',
@@ -903,30 +902,51 @@ $$('[data-view]').forEach((button) => {
   button.onclick = () => switchView(button.dataset.view);
 });
 
-function setMobileNavSheet(open) {
+const MOBILE_VIEW_META = {
+  dashboard: { label: 'לוח בקרה', icon: '⌂' },
+  jobs: { label: 'משרות', icon: '▣' },
+  preferences: { label: 'העדפות חיפוש', icon: '≡' },
+  applications: { label: 'הגשות', icon: '✓' },
+  blockers: { label: 'דורש טיפול', icon: '!' },
+  skills: { label: 'סקילים', icon: '◇' },
+  sources: { label: 'מקורות', icon: '◎' },
+  profile: { label: 'הפרופיל שלי', icon: '◉' },
+};
+
+function updateMobileTabDock(view) {
+  const meta = MOBILE_VIEW_META[view] || MOBILE_VIEW_META.dashboard;
+  const label = $('#mobile-tab-current-label');
+  const icon = $('#mobile-tab-current-icon');
+  if (label) label.textContent = meta.label;
+  if (icon) icon.textContent = meta.icon;
+}
+
+function setMobileTabMenu(open) {
   const backdrop = $('#mobile-nav-backdrop');
-  const more = $('#mobile-more');
-  if (!backdrop) return;
-  const visible = Boolean(open);
+  const dock = $('#mobile-tab-dock');
+  const menu = $('#mobile-tab-menu');
+  const trigger = $('#mobile-tab-trigger');
+  if (!backdrop || !dock || !menu || !trigger) return;
+  const visible = Boolean(open) && window.innerWidth <= 760;
+  if (visible) { closeNotifications(); setCareerMenu(false); }
   backdrop.classList.toggle('open', visible);
   backdrop.setAttribute('aria-hidden', visible ? 'false' : 'true');
-  more?.setAttribute('aria-expanded', String(visible));
+  dock.classList.toggle('open', visible);
+  menu.setAttribute('aria-hidden', visible ? 'false' : 'true');
+  trigger.setAttribute('aria-expanded', String(visible));
   document.body.classList.toggle('mobile-nav-open', visible);
 }
 
 $$('[data-mobile-view]').forEach((button) => {
   button.onclick = () => {
-    setMobileNavSheet(false);
+    setMobileTabMenu(false);
     switchView(button.dataset.mobileView);
   };
 });
-$('#mobile-more').onclick = () => setMobileNavSheet(!$('#mobile-nav-backdrop')?.classList.contains('open'));
-$('#mobile-nav-close').onclick = () => setMobileNavSheet(false);
-$('#mobile-nav-backdrop').addEventListener('click', (event) => {
-  if (event.target.id === 'mobile-nav-backdrop') setMobileNavSheet(false);
-});
+$('#mobile-tab-trigger').onclick = () => setMobileTabMenu(!$('#mobile-tab-dock')?.classList.contains('open'));
+$('#mobile-nav-backdrop').addEventListener('click', () => setMobileTabMenu(false));
 window.addEventListener('resize', () => {
-  if (window.innerWidth > 760) setMobileNavSheet(false);
+  if (window.innerWidth > 760) setMobileTabMenu(false);
 });
 
 function switchProfileSection(section) {
@@ -2921,6 +2941,7 @@ function closeNotifications() {
   $('#notification-trigger').setAttribute('aria-expanded','false');
 }
 $('#notification-trigger').onclick = () => {
+  setMobileTabMenu(false);
   renderNotificationCenter();
   const open = $('#notification-center').classList.toggle('open');
   $('#notification-center').setAttribute('aria-hidden', String(!open));
diff --git a/app/static/index.html b/app/static/index.html
index b619a55..0066564 100644
--- a/app/static/index.html
+++ b/app/static/index.html
@@ -5,7 +5,7 @@
   <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
   <title>JobPilot — חיפוש והגשת משרות חכמה</title>
   <link rel="icon" type="image/svg+xml" href="/static/jobpilot-mark.svg?v=17" />
-  <link rel="stylesheet" href="/static/styles.css?v=0.45.0" />
+  <link rel="stylesheet" href="/static/styles.css?v=0.45.1" />
 </head>
 <body>
   <section id="auth-gate" class="auth-gate" hidden aria-live="polite">
@@ -76,6 +76,23 @@
         <i class="brand-flight-dot" id="brand-flight-dot" aria-hidden="true"></i>
         <div><strong dir="ltr">JobP<span class="brand-i">ı<span class="brand-i-dot" id="brand-i-dot"></span></span>lot</strong><span class="brand-tagline" id="brand-tagline">חיפוש משרות חכם</span></div>
       </div>
+      <nav id="mobile-tab-dock" class="mobile-tab-dock" aria-label="ניווט בין אזורים במובייל">
+        <button id="mobile-tab-trigger" class="mobile-tab-trigger" type="button" aria-expanded="false" aria-controls="mobile-tab-menu">
+          <span id="mobile-tab-current-icon" class="mobile-tab-current-icon" aria-hidden="true">⌂</span>
+          <span class="mobile-tab-current-copy"><small>אתה נמצא ב</small><strong id="mobile-tab-current-label">לוח בקרה</strong></span>
+          <span class="mobile-tab-chevron" aria-hidden="true">⌄</span>
+        </button>
+        <div id="mobile-tab-menu" class="mobile-tab-menu" aria-hidden="true">
+          <button type="button" data-mobile-view="dashboard" data-mobile-icon="⌂" class="active"><span aria-hidden="true">⌂</span><strong>לוח בקרה</strong><small>תמונת מצב כללית</small></button>
+          <button type="button" data-mobile-view="jobs" data-mobile-icon="▣"><span aria-hidden="true">▣</span><strong>משרות</strong><small>התאמות והזדמנויות</small></button>
+          <button type="button" data-mobile-view="preferences" data-mobile-icon="≡"><span aria-hidden="true">≡</span><strong>העדפות חיפוש</strong><small>תפקידים, ניסיון ומיקום</small></button>
+          <button type="button" data-mobile-view="applications" data-mobile-icon="✓"><span aria-hidden="true">✓</span><strong>הגשות</strong><small>מעקב אחר מועמדויות</small></button>
+          <button type="button" data-mobile-view="blockers" data-mobile-icon="!"><span aria-hidden="true">!</span><strong>דורש טיפול</strong><small>פעולות שממתינות לך</small></button>
+          <button type="button" data-mobile-view="skills" data-mobile-icon="◇"><span aria-hidden="true">◇</span><strong>סקילים</strong><small>יכולות ופערי ידע</small></button>
+          <button type="button" data-mobile-view="sources" data-mobile-icon="◎"><span aria-hidden="true">◎</span><strong>מקורות</strong><small>אתרי הקריירה והסריקות</small></button>
+          <button type="button" data-mobile-view="profile" data-mobile-icon="◉"><span aria-hidden="true">◉</span><strong>הפרופיל שלי</strong><small>פרטים ושאלות הגשה</small></button>
+        </div>
+      </nav>
       <nav id="nav">
         <button data-view="dashboard" class="active"><span class="nav-icon"><svg viewBox="0 0 24 24" aria-hidden="true"><rect x="3.5" y="4" width="7" height="6.5" rx="2"/><rect x="13.5" y="4" width="7" height="10" rx="2"/><rect x="3.5" y="13.5" width="7" height="6.5" rx="2"/><path class="nav-accent" d="M14.8 18.5h4.4M17 16.3v4.4"/></svg></span><span class="nav-label">לוח בקרה<small>תמונת מצב כללית</small></span></button>
         <button data-view="jobs"><span class="nav-icon"><svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 7.8h18v10a2.2 2.2 0 0 1-2.2 2.2H5.2A2.2 2.2 0 0 1 3 17.8v-10Z"/><path d="M8.5 7.8V6.2A1.7 1.7 0 0 1 10.2 4.5h3.6a1.7 1.7 0 0 1 1.7 1.7v1.6M3 12c5.2 2.35 12.8 2.35 18 0"/><circle class="nav-accent" cx="12" cy="13" r="1.35"/></svg></span><span class="nav-label">משרות<small>התאמות והזדמנויות</small></span></button>
@@ -405,32 +422,12 @@
     </main>
   </div>
 
-  <nav id="mobile-bottom-nav" class="mobile-bottom-nav" aria-label="ניווט ראשי במובייל">
-    <button type="button" data-mobile-view="dashboard" class="active" aria-label="לוח בקרה"><span aria-hidden="true">⌂</span><small>בית</small></button>
-    <button type="button" data-mobile-view="jobs" aria-label="משרות"><span aria-hidden="true">▣</span><small>משרות</small></button>
-    <button type="button" data-mobile-view="applications" aria-label="הגשות"><span aria-hidden="true">✓</span><small>הגשות</small></button>
-    <button type="button" data-mobile-view="profile" aria-label="הפרופיל שלי"><span aria-hidden="true">◉</span><small>פרופיל</small></button>
-    <button type="button" id="mobile-more" aria-label="עוד אפשרויות" aria-expanded="false"><span aria-hidden="true">•••</span><small>עוד</small></button>
-  </nav>
-
-  <div id="mobile-nav-backdrop" class="mobile-nav-backdrop" aria-hidden="true">
-    <section id="mobile-nav-sheet" class="mobile-nav-sheet" role="dialog" aria-modal="true" aria-label="עוד אזורים ב־JobPilot">
-      <div class="mobile-sheet-handle" aria-hidden="true"></div>
-      <div class="mobile-sheet-head"><div><span class="kicker">JobPilot</span><h2>כלים נוספים</h2></div><button type="button" id="mobile-nav-close" aria-label="סגור">×</button></div>
-      <div class="mobile-menu-grid">
-        <button type="button" data-mobile-view="preferences"><span aria-hidden="true">≡</span><strong>העדפות חיפוש</strong><small>תפקידים, ניסיון ומיקום</small></button>
-        <button type="button" data-mobile-view="blockers"><span aria-hidden="true">!</span><strong>דורש טיפול</strong><small>פעולות שממתינות לך</small></button>
-        <button type="button" data-mobile-view="skills"><span aria-hidden="true">◇</span><strong>סקילים</strong><small>יכולות ופערי ידע</small></button>
-        <button type="button" data-mobile-view="sources"><span aria-hidden="true">◎</span><strong>מקורות</strong><small>אתרי הקריירה והסריקות</small></button>
-      </div>
-      <p class="mobile-sheet-note">הניווט התחתון שומר את האזורים המרכזיים בהישג אגודל; כל שאר הכלים נמצאים כאן בלי להעמיס על המסך.</p>
-    </section>
-  </div>
+  <div id="mobile-nav-backdrop" class="mobile-nav-backdrop" aria-hidden="true"></div>
 
   <div id="modal" class="modal" aria-hidden="true"><div class="modal-card"><button class="modal-close">×</button><div id="modal-content"></div></div></div>
   <aside id="notification-center" class="notification-center" aria-hidden="true" aria-label="מרכז התראות"><div class="notification-head"><div><span class="kicker">עדכונים ופעולות</span><h2>מרכז התראות</h2></div><button type="button" id="notification-close" aria-label="סגור התראות">×</button></div><div id="notification-list" class="notification-list"></div></aside>
   <div id="command-palette" class="command-palette" aria-hidden="true"><div class="command-card" role="dialog" aria-modal="true" aria-label="חיפוש מהיר"><label><span>⌕</span><input id="command-input" type="search" placeholder="חיפוש משרה, חברה, סקיל או מעבר לעמוד…" autocomplete="off" /></label><div id="command-results" class="command-results"></div><small>Enter לבחירה · Esc לסגירה · ⌘K לפתיחה</small></div></div>
   <div id="toast" class="toast" role="status" aria-live="polite"></div>
-  <script src="/static/app.js?v=0.26.0"></script>
+  <script src="/static/app.js?v=0.26.1"></script>
 </body>
 </html>
diff --git a/app/static/styles.css b/app/static/styles.css
index 2dde0ba..b49fba6 100644
--- a/app/static/styles.css
+++ b/app/static/styles.css
@@ -2478,12 +2478,12 @@ body.theme-dark.track-industrial-engineering :where(
 
 /* v0.3.2 — mobile app redesign: mobile is a dedicated information architecture,
    not a compressed desktop canvas. */
-.mobile-bottom-nav,.mobile-nav-backdrop { display:none; }
+.mobile-tab-dock,.mobile-nav-backdrop { display:none; }
 
 @media (max-width:760px) {
   :root { --mobile-safe-bottom:max(10px,env(safe-area-inset-bottom)); }
   html,body { min-height:100%; overflow-x:hidden; }
-  body { padding-bottom:calc(78px + var(--mobile-safe-bottom)); }
+  body { padding-bottom:0; }
 
   .app-shell { display:flex; flex-direction:column; min-height:100dvh; width:100%; }
   .sidebar {
@@ -2492,15 +2492,54 @@ body.theme-dark.track-industrial-engineering :where(
     border-bottom:1px solid var(--line); background:color-mix(in srgb,var(--panel) 92%,transparent);
     box-shadow:0 8px 24px rgba(20,59,88,.08); backdrop-filter:blur(20px);
   }
-  .sidebar .brand { position:relative; inset:auto; width:auto; min-width:0; gap:8px; }
+  .sidebar .brand { position:relative; inset:auto; width:42px; min-width:42px; min-height:42px; flex:0 0 42px; gap:0; padding:0; }
   .sidebar .brand-mark { width:42px; height:42px; flex:0 0 42px; border-radius:13px; }
-  .sidebar .brand strong { font-size:18px; line-height:1; }
-  .sidebar .brand-tagline { margin-top:2px; font-size:8px; white-space:nowrap; }
-  .sidebar .brand-flight-dot { transform:scale(.72); transform-origin:center; }
+  .sidebar .brand > div,.sidebar .brand-flight-dot { display:none !important; }
   .sidebar #nav,.sidebar-footer { display:none !important; }
-  .dock-utility { position:static !important; inset:auto !important; display:flex; margin:0; }
+  .dock-utility { position:static !important; inset:auto !important; display:flex; margin:0; margin-inline-start:auto; flex:0 0 auto; }
   .dock-utility .icon-action { width:42px; height:42px; border-radius:13px; }
 
+  /* The current tab is the mobile dock: a sticky top-right pill with a downward floating menu. */
+  .mobile-tab-dock {
+    position:relative; z-index:82; order:-1; display:block; width:min(174px,calc(100vw - 138px)); min-width:126px; flex:0 1 174px;
+  }
+  .mobile-tab-trigger {
+    width:100%; height:46px; display:grid; grid-template-columns:30px minmax(0,1fr) 18px; align-items:center; gap:7px;
+    padding:5px 9px; border:1px solid color-mix(in srgb,var(--brand) 24%,var(--line)); border-radius:15px;
+    background:color-mix(in srgb,var(--panel) 95%,transparent); color:var(--ink); box-shadow:0 9px 24px rgba(10,42,65,.13);
+    backdrop-filter:blur(20px); -webkit-backdrop-filter:blur(20px); text-align:right; font:inherit; cursor:pointer;
+  }
+  .mobile-tab-current-icon { width:30px; height:30px; display:grid; place-items:center; border-radius:10px; background:var(--brand-soft); color:var(--brand); font-size:16px; font-weight:900; }
+  .mobile-tab-current-copy { min-width:0; display:grid; gap:0; line-height:1.08; }
+  .mobile-tab-current-copy small { color:var(--muted); font-size:7px; font-weight:700; }
+  .mobile-tab-current-copy strong { overflow:hidden; text-overflow:ellipsis; white-space:nowrap; font-size:10px; font-weight:850; }
+  .mobile-tab-chevron { display:grid; place-items:center; color:var(--muted); font-size:15px; transition:transform .2s var(--ease-apple-out); }
+  .mobile-tab-dock.open .mobile-tab-chevron { transform:rotate(180deg); }
+  .mobile-tab-dock.open .mobile-tab-trigger { border-color:color-mix(in srgb,var(--brand) 45%,var(--line)); box-shadow:0 14px 34px rgba(10,42,65,.18); }
+  .mobile-tab-menu {
+    position:absolute; top:calc(100% + 9px); right:0; width:min(286px,calc(100vw - 24px)); max-height:calc(100dvh - 86px); overflow:auto;
+    display:grid; gap:5px; padding:8px; border:1px solid var(--line); border-radius:19px; background:color-mix(in srgb,var(--panel) 97%,transparent);
+    box-shadow:0 22px 58px rgba(5,28,45,.24); backdrop-filter:blur(24px); -webkit-backdrop-filter:blur(24px);
+    opacity:0; visibility:hidden; pointer-events:none; transform:translateY(-7px) scale(.98); transform-origin:top right;
+    transition:opacity .16s ease,transform .2s var(--ease-apple-out),visibility .16s ease; overscroll-behavior:contain;
+  }
+  .mobile-tab-dock.open .mobile-tab-menu { opacity:1; visibility:visible; pointer-events:auto; transform:none; }
+  .mobile-tab-menu button {
+    width:100%; min-height:52px; display:grid; grid-template-columns:34px minmax(0,1fr); grid-template-rows:auto auto; column-gap:9px;
+    align-items:center; padding:8px 9px; border:1px solid transparent; border-radius:14px; background:transparent; color:var(--ink); text-align:right; font:inherit; cursor:pointer;
+  }
+  .mobile-tab-menu button > span { grid-row:1 / span 2; width:34px; height:34px; display:grid; place-items:center; border-radius:11px; background:var(--surface-soft); color:var(--muted); font-size:15px; font-weight:900; }
+  .mobile-tab-menu button strong { font-size:10px; line-height:1.2; }
+  .mobile-tab-menu button small { color:var(--muted); font-size:8px; line-height:1.3; }
+  .mobile-tab-menu button.active { border-color:color-mix(in srgb,var(--brand) 34%,var(--line)); background:color-mix(in srgb,var(--brand-soft) 66%,var(--panel)); }
+  .mobile-tab-menu button.active > span { background:var(--brand-soft); color:var(--brand); }
+  .mobile-nav-backdrop {
+    position:fixed; z-index:39; inset:0; display:block; background:rgba(3,16,26,.10); opacity:0; visibility:hidden; pointer-events:none;
+    backdrop-filter:blur(1.5px); -webkit-backdrop-filter:blur(1.5px); transition:opacity .16s ease,visibility .16s ease;
+  }
+  .mobile-nav-backdrop.open { opacity:1; visibility:visible; pointer-events:auto; }
+  body.mobile-nav-open { overflow:hidden; }
+
   main { order:1; width:100%; min-width:0; padding:10px 12px 22px; text-align:right; }
   .topbar {
     position:relative; z-index:10; display:flex; flex-direction:column; align-items:stretch; gap:10px;
@@ -2632,53 +2671,13 @@ body.theme-dark.track-industrial-engineering :where(
   .auth-google,.auth-submit,.auth-guest { min-height:50px; }
   .auth-footnote { padding-inline:8px; font-size:8px; line-height:1.45; }
 
-  /* Fixed bottom navigation for thumb reach. */
-  .mobile-bottom-nav {
-    position:fixed; z-index:60; right:10px; left:10px; bottom:var(--mobile-safe-bottom); height:64px;
-    display:grid; grid-template-columns:repeat(5,minmax(0,1fr)); align-items:center; gap:2px; padding:5px;
-    border:1px solid color-mix(in srgb,var(--line) 88%,transparent); border-radius:22px;
-    background:color-mix(in srgb,var(--panel) 94%,transparent); box-shadow:0 16px 42px rgba(10,42,65,.19); backdrop-filter:blur(22px);
-  }
-  .mobile-bottom-nav button {
-    min-width:0; height:54px; display:grid; place-items:center; align-content:center; gap:2px; padding:2px;
-    border:0; border-radius:17px; background:transparent; color:var(--muted); font:inherit;
-  }
-  .mobile-bottom-nav button > span { min-width:28px; height:28px; display:grid; place-items:center; border-radius:10px; font-size:17px; font-weight:850; }
-  .mobile-bottom-nav button small { font-size:8px; font-weight:780; }
-  .mobile-bottom-nav button.active { color:var(--brand); }
-  .mobile-bottom-nav button.active > span { background:var(--brand-soft); color:var(--brand); }
-
-  .mobile-nav-backdrop {
-    position:fixed; z-index:70; inset:0; display:flex; align-items:flex-end; justify-content:center; padding:0;
-    background:rgba(3,16,26,.38); opacity:0; visibility:hidden; pointer-events:none; transition:opacity .2s ease,visibility .2s ease;
-  }
-  .mobile-nav-backdrop.open { opacity:1; visibility:visible; pointer-events:auto; }
-  .mobile-nav-sheet {
-    width:100%; padding:8px 14px calc(18px + env(safe-area-inset-bottom)); border:1px solid var(--line); border-bottom:0;
-    border-radius:26px 26px 0 0; background:var(--panel); box-shadow:0 -18px 50px rgba(5,24,38,.2);
-    transform:translateY(22px); transition:transform .24s var(--ease-apple-out);
-  }
-  .mobile-nav-backdrop.open .mobile-nav-sheet { transform:translateY(0); }
-  .mobile-sheet-handle { width:44px; height:4px; margin:0 auto 10px; border-radius:999px; background:var(--line-strong); }
-  .mobile-sheet-head { display:flex; align-items:center; justify-content:space-between; gap:10px; margin-bottom:10px; }
-  .mobile-sheet-head h2 { margin:1px 0 0; font-size:18px; }
-  .mobile-sheet-head > button { width:36px; height:36px; border:1px solid var(--line); border-radius:12px; background:var(--surface-soft); color:var(--ink); font-size:20px; }
-  .mobile-menu-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:8px; }
-  .mobile-menu-grid button { min-width:0; min-height:84px; display:grid; grid-template-columns:34px minmax(0,1fr); grid-template-rows:auto auto; column-gap:8px; align-items:center; padding:10px; text-align:right; border:1px solid var(--line); border-radius:17px; background:var(--surface-soft); color:var(--ink); font:inherit; }
-  .mobile-menu-grid button > span { grid-row:1 / span 2; width:34px; height:34px; display:grid; place-items:center; border-radius:11px; background:var(--brand-soft); color:var(--brand); font-weight:900; }
-  .mobile-menu-grid strong { font-size:10px; }
-  .mobile-menu-grid small { color:var(--muted); font-size:8px; line-height:1.35; }
-  .mobile-menu-grid button.active { border-color:color-mix(in srgb,var(--brand) 45%,var(--line)); background:color-mix(in srgb,var(--brand-soft) 62%,var(--panel)); }
-  .mobile-sheet-note { margin:10px 2px 0; color:var(--muted); font-size:8px; line-height:1.45; }
-  body.mobile-nav-open { overflow:hidden; }
-
-  .notification-center { position:fixed; right:10px; left:10px; bottom:calc(78px + var(--mobile-safe-bottom)); width:auto; max-height:65dvh; border-radius:20px; }
+  .notification-center { position:fixed; right:10px; left:10px; top:74px; bottom:auto; width:auto; max-height:calc(100dvh - 88px); border-radius:20px; }
 }
 
-body.theme-dark .mobile-bottom-nav,body.theme-dark .mobile-nav-sheet { background:color-mix(in srgb,var(--panel) 95%,#071522); }
-body.track-industrial-engineering .mobile-bottom-nav button.active,body.track-industrial-engineering .mobile-menu-grid button > span { color:var(--accent); }
-body.track-industrial-engineering .mobile-bottom-nav button.active > span { background:var(--accent-soft); color:var(--accent); }
-body.track-industrial-engineering .mobile-menu-grid button.active { border-color:var(--accent-border); background:var(--accent-soft-2); }
+body.theme-dark .mobile-tab-trigger,body.theme-dark .mobile-tab-menu { background:color-mix(in srgb,var(--panel) 96%,#071522); }
+body.track-industrial-engineering .mobile-tab-current-icon,body.track-industrial-engineering .mobile-tab-menu button.active > span { color:var(--accent); }
+body.track-industrial-engineering .mobile-tab-current-icon,body.track-industrial-engineering .mobile-tab-menu button.active > span { background:var(--accent-soft); color:var(--accent); }
+body.track-industrial-engineering .mobile-tab-menu button.active { border-color:var(--accent-border); background:var(--accent-soft-2); }
 
 /* Shared admin catalog has no guest-owned application status to filter by. */
 body.guest-mode .job-status-control { display:none !important; }
diff --git a/tests/test_api.py b/tests/test_api.py
index 3ef0f5d..0a927bf 100644
--- a/tests/test_api.py
+++ b/tests/test_api.py
@@ -259,7 +259,7 @@ def test_frontend_assets_are_never_stale_after_an_update():
         assert "no-store" in index.headers["cache-control"]
         assert "no-store" in script.headers["cache-control"]
         assert "no-store" in stylesheet.headers["cache-control"]
-        assert "app.js?v=0.26.0" in index.text
+        assert "app.js?v=0.26.1" in index.text
         assert "הנתון לא נשמר עדיין" in script.text
 
 
diff --git a/tests/test_career_track_browser_offline.py b/tests/test_career_track_browser_offline.py
index 0601cb5..bb90fe3 100644
--- a/tests/test_career_track_browser_offline.py
+++ b/tests/test_career_track_browser_offline.py
@@ -26,8 +26,8 @@ def test_real_browser_switches_profession_theme_options_and_agent_state():
     html = (ROOT / "app" / "static" / "index.html").read_text()
     css = (ROOT / "app" / "static" / "styles.css").read_text()
     js = (ROOT / "app" / "static" / "app.js").read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.45.0" />', f"<style>{css}</style>")
-    html = html.replace('<script src="/static/app.js?v=0.26.0"></script>', "")
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.45.1" />', f"<style>{css}</style>")
+    html = html.replace('<script src="/static/app.js?v=0.26.1"></script>', "")
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=["--no-sandbox"])
diff --git a/tests/test_cloud_ui_offline.py b/tests/test_cloud_ui_offline.py
index 723effe..a7a9485 100644
--- a/tests/test_cloud_ui_offline.py
+++ b/tests/test_cloud_ui_offline.py
@@ -28,8 +28,8 @@ def test_cloud_session_hides_login_and_shows_account_and_agent_state():
     html = (ROOT / "app" / "static" / "index.html").read_text()
     css = (ROOT / "app" / "static" / "styles.css").read_text()
     js = (ROOT / "app" / "static" / "app.js").read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.42.0" />', f"<style>{css}</style>")
-    html = html.replace('<script src="/static/app.js?v=0.26.0"></script>', "")
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.45.1" />', f"<style>{css}</style>")
+    html = html.replace('<script src="/static/app.js?v=0.26.1"></script>', "")
     session = {"access_token": _fake_jwt(), "refresh_token": "refresh", "token_type": "bearer"}
 
     with sync_playwright() as playwright:
@@ -85,7 +85,7 @@ def test_cloud_without_session_shows_login_gate():
     chromium = _chromium_path()
     if not chromium:
         pytest.skip("No system Chromium executable")
-    html = (ROOT / "app" / "static" / "index.html").read_text().replace('<script src="/static/app.js?v=0.26.0"></script>', "")
+    html = (ROOT / "app" / "static" / "index.html").read_text().replace('<script src="/static/app.js?v=0.26.1"></script>', "")
     js = (ROOT / "app" / "static" / "app.js").read_text()
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=["--no-sandbox"])
@@ -118,8 +118,8 @@ def test_cloud_non_owner_cannot_pair_or_enable_application_agent():
     html = (ROOT / "app" / "static" / "index.html").read_text()
     css = (ROOT / "app" / "static" / "styles.css").read_text()
     js = (ROOT / "app" / "static" / "app.js").read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.42.0" />', f"<style>{css}</style>")
-    html = html.replace('<script src="/static/app.js?v=0.26.0"></script>', "")
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.45.1" />', f"<style>{css}</style>")
+    html = html.replace('<script src="/static/app.js?v=0.26.1"></script>', "")
     session = {"access_token": _fake_jwt(), "refresh_token": "refresh", "token_type": "bearer"}
 
     with sync_playwright() as playwright:
diff --git a/tests/test_dashboard_metrics_compact.py b/tests/test_dashboard_metrics_compact.py
index 8b98703..5154e60 100644
--- a/tests/test_dashboard_metrics_compact.py
+++ b/tests/test_dashboard_metrics_compact.py
@@ -26,5 +26,5 @@ def test_metrics_stay_compact_on_mobile_and_support_dark_mode():
 
 
 def test_v0111_asset_versions_are_bumped():
-    assert 'styles.css?v=0.45.0' in HTML
-    assert 'app.js?v=0.26.0' in HTML
+    assert 'styles.css?v=0.45.1' in HTML
+    assert 'app.js?v=0.26.1' in HTML
diff --git a/tests/test_google_oauth_contract.py b/tests/test_google_oauth_contract.py
index 34888f9..fa07c0f 100644
--- a/tests/test_google_oauth_contract.py
+++ b/tests/test_google_oauth_contract.py
@@ -19,4 +19,4 @@ def test_google_oauth_callback_surfaces_provider_errors_from_hash_or_query():
 
 
 def test_google_oauth_asset_version_is_bumped():
-    assert 'app.js?v=0.26.0' in HTML
+    assert 'app.js?v=0.26.1' in HTML
diff --git a/tests/test_scoped_save_browser_offline.py b/tests/test_scoped_save_browser_offline.py
index fa9f329..50331c2 100644
--- a/tests/test_scoped_save_browser_offline.py
+++ b/tests/test_scoped_save_browser_offline.py
@@ -20,8 +20,8 @@ def test_each_profile_card_saves_only_its_dirty_fields_and_keeps_other_drafts():
     html = (ROOT / 'app' / 'static' / 'index.html').read_text()
     css = (ROOT / 'app' / 'static' / 'styles.css').read_text()
     js = (ROOT / 'app' / 'static' / 'app.js').read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.42.0" />', f'<style>{css}</style>')
-    html = html.replace('<script src="/static/app.js?v=0.26.0"></script>', '')
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.45.1" />', f'<style>{css}</style>')
+    html = html.replace('<script src="/static/app.js?v=0.26.1"></script>', '')
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=['--no-sandbox'])
diff --git a/tests/test_ui_offline.py b/tests/test_ui_offline.py
index f815986..0061bf0 100644
--- a/tests/test_ui_offline.py
+++ b/tests/test_ui_offline.py
@@ -19,7 +19,7 @@ def test_delete_buttons_and_israel_scan_report_in_real_browser_without_server():
         pytest.skip("No system Chromium executable")
 
     html = (PROJECT_ROOT / "app" / "static" / "index.html").read_text()
-    html = html.replace('<script src="/static/app.js?v=0.26.0"></script>', "")
+    html = html.replace('<script src="/static/app.js?v=0.26.1"></script>', "")
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(
@@ -108,7 +108,7 @@ def test_compact_blockers_and_handoff_links_render_inside_application_queue():
         pytest.skip("No system Chromium executable")
 
     html = (PROJECT_ROOT / "app" / "static" / "index.html").read_text()
-    html = html.replace('<script src="/static/app.js?v=0.26.0"></script>', "")
+    html = html.replace('<script src="/static/app.js?v=0.26.1"></script>', "")
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=["--no-sandbox"])
diff --git a/tests/test_ui_polish_contract.py b/tests/test_ui_polish_contract.py
index dba5134..2ff4397 100644
--- a/tests/test_ui_polish_contract.py
+++ b/tests/test_ui_polish_contract.py
@@ -72,19 +72,30 @@ def test_mobile_layout_has_explicit_rtl_vertical_flow():
     assert '.auth-form input[type="email"],.auth-password-field input { direction:ltr; text-align:left; }' in CSS
 
 
-def test_mobile_redesign_uses_bottom_navigation_sheet_and_phone_first_job_layout():
-    assert 'id="mobile-bottom-nav"' in HTML
-    assert 'id="mobile-nav-sheet"' in HTML
+def test_mobile_redesign_uses_top_right_floating_tab_dock_and_phone_first_job_layout():
+    assert 'id="mobile-tab-dock"' in HTML
+    assert 'id="mobile-tab-trigger"' in HTML
+    assert 'id="mobile-tab-menu"' in HTML
+    assert 'id="mobile-bottom-nav"' not in HTML
     assert 'data-mobile-view="jobs"' in HTML
+    assert 'data-mobile-view="profile"' in HTML
     assert 'viewport-fit=cover' in HTML
     assert '/* v0.3.2 — mobile app redesign:' in CSS
-    assert '.mobile-bottom-nav {' in CSS
-    assert 'position:fixed; z-index:60;' in CSS
+    assert '/* The current tab is the mobile dock:' in CSS
+    assert '.mobile-tab-dock {' in CSS
+    assert 'position:relative; z-index:82;' in CSS
+    assert '.mobile-tab-menu {' in CSS
+    assert 'top:calc(100% + 9px); right:0;' in CSS
+    assert '.mobile-nav-backdrop {' in CSS
+    assert 'z-index:39;' in CSS
     assert '.jobs-toolbar {' in CSS
     assert 'position:sticky; top:72px;' in CSS
     assert '.modal { align-items:flex-end; justify-content:center; padding:0; }' in CSS
-    assert "function setMobileNavSheet(open)" in JS
+    assert 'function setMobileTabMenu(open)' in JS
+    assert 'function updateMobileTabDock(view)' in JS
+    assert 'if (visible) { closeNotifications(); setCareerMenu(false); }' in JS
+    assert "setMobileTabMenu(false);\n  renderNotificationCenter();" in JS
     assert "$$('[data-mobile-view]')" in JS
     assert "function jobCardActions(job)" in JS
-    assert 'app.js?v=0.26.0' in HTML
-    assert 'styles.css?v=0.45.0' in HTML
+    assert 'app.js?v=0.26.1' in HTML
+    assert 'styles.css?v=0.45.1' in HTML
