diff --git a/app/main.py b/app/main.py
index ed4b9ec..548ebb2 100644
--- a/app/main.py
+++ b/app/main.py
@@ -536,9 +536,9 @@ def _job_payload_for_request(job: Job, request: Request, *, full: bool = False,
 @app.get("/api/admin/users")
 def admin_users(request: Request, db: Session = Depends(get_db)):
     identity = getattr(request.state, "identity", None)
-    if settings.auth_mode != "supabase" or not identity or identity.role != "admin":
+    if not _developer_tools_allowed(identity):
         raise HTTPException(403, "Admin access required")
-    accounts = db.scalars(select(AppIdentity).order_by(AppIdentity.id)).all()
+    accounts = db.scalars(select(AppIdentity).order_by(desc(AppIdentity.last_seen_at), AppIdentity.id)).all()
     return {
         "count": len(accounts),
         "max_users": max(1, int(settings.max_users or 10)),
diff --git a/app/static/app.js b/app/static/app.js
index d0fbc50..c6938f4 100644
--- a/app/static/app.js
+++ b/app/static/app.js
@@ -3105,7 +3105,8 @@ $('#resume-file').onchange = async (event) => {
   formData.append('file', file);
   try {
     const result = await api('/api/profile/resume', { method: 'POST', body: formData });
-    $('#resume-name').textContent = result.filename;
+    $('#resume-name').textContent = `✓ ${result.filename}`;
+    document.querySelector('.resume-profile-card')?.classList.add('resume-uploaded');
     if (state.profile) state.profile.cv_filename = result.filename;
     state.profile=result.profile||state.profile; state.profileLoaded=false;
     await loadProfile();
@@ -3332,106 +3333,120 @@ document.addEventListener('keydown', (event) => { if (event.key === 'Escape') se
 
 
 const ONBOARDING_VERSION = 2;
-const onboardingState = { step: 0, preview: false, resume: null, selectedSkills: new Set(), draft: {} };
-const onboardingSteps = ['welcome','resume','preferences','review','scan'];
+const onboardingState = { step: 0, preview: false, resume: null, selectedSkills: new Set(), draft: {}, scanTimer: null };
+const onboardingSteps = ['track','resume','skills','preferences','review','scan'];
 
 function onboardingSplit(value=''){ return String(value).split(',').map(v=>v.trim()).filter(Boolean); }
+function onboardingTrackConfig(key=state.activeCareerTrack){ return CAREER_TRACK_UI[key] || CAREER_TRACK_UI.computer_science; }
+function onboardingPresetSkills(){ return (onboardingTrackConfig().skills||[]).map(([value])=>value); }
+function onboardingSkillValues(){ return [...new Set([...onboardingPresetSkills(), ...onboardingState.selectedSkills])]; }
+function onboardingApplyTrackTheme(key){ state.activeCareerTrack=key; applyCareerTrackTheme(); document.querySelector('.onboarding-shell')?.setAttribute('data-track',key); }
+async function onboardingChooseTrack(key){
+  if(!key || key===state.activeCareerTrack){ onboardingApplyTrackTheme(key||state.activeCareerTrack); onboardingSetStep(1); return; }
+  const result=await api('/api/career-tracks/active',{method:'PUT',body:JSON.stringify({track:key})});
+  state.activeCareerTrack=result.active_track||key; state.careerTracks=result.tracks||state.careerTracks; state.profile=result.profile||state.profile;
+  onboardingApplyTrackTheme(state.activeCareerTrack); renderCareerPreferenceOptions(); renderCareerSwitcher(); onboardingState.selectedSkills=new Set(); onboardingSetStep(1);
+}
 function onboardingSetStep(index){
   onboardingState.step=Math.max(0,Math.min(onboardingSteps.length-1,index));
-  const step=onboardingSteps[onboardingState.step], profile=state.profile||{}, track=CAREER_TRACK_UI[state.activeCareerTrack]||CAREER_TRACK_UI.computer_science;
-  $('#onboarding-progress-label').textContent=`${onboardingState.step+1} מתוך 5`;
-  $('#onboarding-progress-bar').style.width=`${(onboardingState.step+1)*20}%`;
-  $('#onboarding-back').hidden=onboardingState.step===0;
+  const step=onboardingSteps[onboardingState.step], profile=state.profile||{}, track=onboardingTrackConfig();
+  $('#onboarding-progress-label').textContent=`${onboardingState.step+1} מתוך ${onboardingSteps.length}`;
+  $('#onboarding-progress-bar').style.width=`${((onboardingState.step+1)/onboardingSteps.length)*100}%`;
+  $('#onboarding-back').hidden=onboardingState.step===0 || step==='scan';
   $('#onboarding-skip').hidden=onboardingState.preview || step==='scan';
-  $('#onboarding-next').hidden=step==='scan';
+  $('#onboarding-next').hidden=step==='track' || step==='scan';
   const content=$('#onboarding-content');
-  if(step==='welcome'){
-    content.innerHTML=`<div class="onboarding-hero"><span class="kicker">ברוכים הבאים</span><h1 id="onboarding-title">נכין את JobPilot לחיפוש שלך</h1><p>קורות חיים, סקילים והעדפות — כמה צעדים קצרים ואז אפשר לצאת לסריקה הראשונה.</p><div class="onboarding-promise"><span>≈ דקה</span><span>אפשר לערוך הכול אחר כך</span><span>בלי שאלון ארוך</span></div></div>`;
+  if(step==='track'){
+    const tracks=(state.careerTracks||[]).filter(t=>CAREER_TRACK_UI[t.key]);
+    content.innerHTML=`<div class="onboarding-hero compact"><span class="kicker">מתחילים מהכיוון שלך</span><h1 id="onboarding-title">באיזה תחום מחפשים את התפקיד הבא?</h1><p>הבחירה מתאימה מיד את הצבעים, הסקילים, המקורות והעדפות החיפוש. המבנה מוכן למסלולים נוספים בהמשך.</p><div class="onboarding-track-grid">${tracks.map(t=>{const ui=onboardingTrackConfig(t.key);return `<button class="onboarding-track-card ${t.key===state.activeCareerTrack?'active':''}" type="button" data-ob-track="${esc(t.key)}"><span class="onboarding-track-symbol">${esc(ui.symbol)}</span><span><strong>${esc(t.label||ui.label)}</strong><small>${esc(t.description||ui.description)}</small></span><i>בחירה</i></button>`}).join('')}</div></div>`;
+    $$('[data-ob-track]',content).forEach(b=>b.onclick=()=>onboardingChooseTrack(b.dataset.obTrack));
   }else if(step==='resume'){
-    const skills=[...onboardingState.selectedSkills];
-    content.innerHTML=`<span class="kicker">1 · קורות חיים</span><h1 id="onboarding-title">נתחיל ממה שכבר כתבת</h1><p>העלה קורות חיים ונציע סקילים שמצאנו. שום סקיל לא יתווסף בלי אישור.</p><label class="onboarding-upload"><input id="onboarding-resume-file" type="file" accept=".pdf,.doc,.docx,.txt,.rtf"><strong>${onboardingState.resume?`✓ ${esc(onboardingState.resume.filename||'קורות החיים הועלו')}`:'בחר קובץ קורות חיים'}</strong><small>PDF, DOCX, DOC, TXT או RTF · עד 10MB</small></label>${skills.length?`<div class="onboarding-skill-pick"><strong>סקילים שמצאנו</strong><p>לחץ כדי לבחור או להסיר.</p><div class="onboarding-chips">${skills.map(v=>`<button type="button" class="onboarding-chip selected" data-ob-skill="${encodeURIComponent(v)}">${esc(v)}</button>`).join('')}</div></div>`:''}`;
+    const uploaded=onboardingState.resume;
+    content.innerHTML=`<span class="kicker">קורות חיים</span><h1 id="onboarding-title">נכיר את הניסיון שלך</h1><p>העלה PDF או DOCX. JobPilot יקרא את הקובץ ויציע סקילים — שום סקיל לא מתווסף בלי בחירה שלך.</p><label class="onboarding-upload ${uploaded?'uploaded':''}"><input id="onboarding-resume-file" type="file" accept=".pdf,.docx,.doc,.txt,.rtf"><span class="onboarding-upload-icon">${uploaded?'✓':'↑'}</span><strong>${uploaded?esc(uploaded.filename||'קורות החיים הועלו'):'בחר קובץ או גרור לכאן'}</strong><small>${uploaded?'הקובץ נקלט ונותח בהצלחה · אפשר להחליף אותו':'PDF או DOCX מומלצים · עד 10MB'}</small>${uploaded?'<em>מוכן להמשך</em>':''}</label>`;
     $('#onboarding-resume-file').onchange=onboardingResume;
-    $$('[data-ob-skill]',content).forEach(b=>b.onclick=()=>b.classList.toggle('selected'));
+  }else if(step==='skills'){
+    const values=onboardingSkillValues();
+    content.innerHTML=`<span class="kicker">סקילים · ${esc(track.label)}</span><h1 id="onboarding-title">מה באמת מייצג אותך?</h1><p>סימנו הצעות שנמצאו בקורות החיים. הוסף או הסר בלחיצה — הרשימה מותאמת למסלול שבחרת.</p><div class="onboarding-skill-checks">${values.map(v=>`<label class="onboarding-skill-check ${onboardingState.selectedSkills.has(v)?'selected':''}"><input type="checkbox" data-ob-skill="${encodeURIComponent(v)}" ${onboardingState.selectedSkills.has(v)?'checked':''}><span class="checkmark">✓</span><strong>${esc(v)}</strong></label>`).join('')}</div>`;
+    $$('[data-ob-skill]',content).forEach(input=>input.onchange=()=>{const skill=decodeURIComponent(input.dataset.obSkill||'');input.closest('.onboarding-skill-check').classList.toggle('selected',input.checked);input.checked?onboardingState.selectedSkills.add(skill):onboardingState.selectedSkills.delete(skill)});
   }else if(step==='preferences'){
     const modes=new Set(profile.preferred_work_modes||['hybrid','remote','onsite']);
-    content.innerHTML=`<span class="kicker">2 · העדפות</span><h1 id="onboarding-title">מה אתה מחפש?</h1><p>חמישה פרטים שעוזרים ל־JobPilot לסנן ולדרג טוב יותר.</p><div class="onboarding-form">
+    content.innerHTML=`<span class="kicker">העדפות · ${esc(track.label)}</span><h1 id="onboarding-title">נחדד את החיפוש</h1><p>כמה פרטים קצרים שמשפיעים ישירות על הסינון והדירוג.</p><div class="onboarding-form polished">
       <label><span>תפקידים רצויים</span><input id="ob-titles" value="${esc((profile.desired_titles||[]).join(', '))}" placeholder="${esc(track.desiredPlaceholder||'Software Engineer')}"></label>
       <label><span>אזורי חיפוש</span><input id="ob-locations" value="${esc((profile.preferred_locations||[]).join(', ')||profile.location||'Israel')}" placeholder="חיפה, תל אביב, מרכז"></label>
       <label><span>רמת ניסיון</span><select id="ob-experience">${['0','1','2','3','4','5+'].map(v=>`<option ${(profile.years_experience_options||['0']).includes(v)?'selected':''}>${v}</option>`).join('')}</select></label>
       <fieldset><legend>אופן עבודה</legend><div class="onboarding-chips">${[['hybrid','היברידי'],['remote','מרחוק'],['onsite','מהמשרד']].map(([v,l])=>`<button type="button" class="onboarding-chip ${modes.has(v)?'selected':''}" data-ob-mode="${v}">${l}</button>`).join('')}</div></fieldset>
-      <label><span>Must-have</span><input id="ob-keywords" value="${esc((profile.keywords||[]).join(', '))}" placeholder="Python, Backend, Algorithms"></label>
-      <label><span>Don't want</span><input id="ob-excluded" value="${esc((profile.excluded_keywords||[]).join(', '))}" placeholder="Support, Senior, Sales"></label>
+      <label><span>חשוב לי למצוא</span><input id="ob-keywords" value="${esc((profile.keywords||[]).join(', '))}" placeholder="טכנולוגיות, תחומים או מאפיינים חשובים"></label>
+      <label><span>לא מתאים לי</span><input id="ob-excluded" value="${esc((profile.excluded_keywords||[]).join(', '))}" placeholder="תפקידים או תחומים שלא תרצה לראות"></label>
     </div>`;
     $$('[data-ob-mode]',content).forEach(b=>b.onclick=()=>b.classList.toggle('selected'));
   }else if(step==='review'){
     const d=onboardingState.draft;
-    content.innerHTML=`<span class="kicker">3 · סיכום</span><h1 id="onboarding-title">כמעט מוכנים</h1><p>אלה הנתונים שישמשו את הסריקה והדירוג.</p><div class="onboarding-summary">
+    content.innerHTML=`<span class="kicker">סיכום</span><h1 id="onboarding-title">החיפוש שלך מוכן</h1><p>זה הפרופיל ש־JobPilot ישתמש בו לסריקה ולדירוג. אפשר לשנות הכול אחר כך.</p><div class="onboarding-review-hero"><span class="onboarding-track-symbol">${esc(track.symbol)}</span><div><strong>${esc(track.label)}</strong><small>${onboardingState.selectedSkills.size} סקילים נבחרו</small></div></div><div class="onboarding-summary refined">
       <article><small>תפקידים</small><strong>${esc((d.desired_titles||[]).join(' · ')||'לא הוגדר')}</strong></article><article><small>מיקום</small><strong>${esc((d.preferred_locations||[]).join(' · ')||'ישראל')}</strong></article>
-      <article><small>אופן עבודה</small><strong>${esc((d.preferred_work_modes||[]).join(' · ')||'לא הוגדר')}</strong></article><article><small>ניסיון</small><strong>${esc((d.years_experience_options||[]).join(' · '))} שנים</strong></article>
-      <article><small>חשוב לי</small><strong>${esc((d.keywords||[]).join(' · ')||'—')}</strong></article><article><small>לא מתאים לי</small><strong>${esc((d.excluded_keywords||[]).join(' · ')||'—')}</strong></article></div><p class="onboarding-note">אפשר לשנות הכול אחר כך בהעדפות חיפוש.</p>`;
+      <article><small>אופן עבודה</small><strong>${esc((d.preferred_work_modes||[]).map(v=>({hybrid:'היברידי',remote:'מרחוק',onsite:'מהמשרד'}[v]||v)).join(' · ')||'לא הוגדר')}</strong></article><article><small>ניסיון</small><strong>${esc((d.years_experience_options||[]).join(' · '))} שנים</strong></article>
+      <article><small>חשוב לי</small><strong>${esc((d.keywords||[]).join(' · ')||'—')}</strong></article><article><small>לא מתאים לי</small><strong>${esc((d.excluded_keywords||[]).join(' · ')||'—')}</strong></article></div>`;
   }else{
-    content.innerHTML=`<span class="kicker">4 · מוכנים</span><h1 id="onboarding-title">רוצה לבצע סריקה ראשונה?</h1><p>הפרופיל והעדפות החיפוש מוכנים. אפשר להתחיל עכשיו או להיכנס לאתר ולסרוק אחר כך.</p><div class="onboarding-ready"><span>✓ קורות חיים וסקילים</span><span>✓ העדפות חיפוש</span><span>✓ דירוג מותאם</span></div><button class="btn primary onboarding-scan" id="onboarding-start-scan" type="button">התחל סריקה ראשונה</button><button class="btn secondary onboarding-enter" id="onboarding-enter-site" type="button">כניסה לאתר בלי סריקה</button>`;
+    content.innerHTML=`<div class="onboarding-scan-stage"><span class="kicker">הכול מוכן</span><h1 id="onboarding-title">נמצא את ההזדמנויות שמתאימות לך</h1><p>הסריקה עוברת על המקורות הפעילים של ${esc(track.label)} ומדרגת את התוצאות לפי הפרופיל שבנית.</p><div class="onboarding-radar" aria-hidden="true"><i></i><i></i><i></i><b></b></div><div id="onboarding-scan-copy" class="onboarding-scan-copy">מוכן לסריקה הראשונה</div><button class="btn primary onboarding-scan" id="onboarding-start-scan" type="button">התחל סריקה ראשונה</button><button class="btn secondary onboarding-enter" id="onboarding-enter-site" type="button">אכנס לאתר ואסרוק אחר כך</button></div>`;
     $('#onboarding-start-scan').onclick=onboardingStartScan; $('#onboarding-enter-site').onclick=()=>onboardingFinish();
   }
 }
 async function onboardingResume(event){
   const file=event.target.files?.[0]; if(!file)return;
+  const allowed=['.pdf','.docx','.doc','.txt','.rtf'], suffix='.'+(file.name.split('.').pop()||'').toLowerCase();
+  if(!allowed.includes(suffix)){toast('אפשר להעלות PDF, DOCX, DOC, TXT או RTF');event.target.value='';return}
+  const label=event.target.closest('.onboarding-upload'); label?.classList.add('uploading');
   const body=new FormData(); body.append('file',file);
   try{
-    const result=await api('/api/profile/resume',{method:'POST',body});
-    onboardingState.resume=result;
-    state.profile=result.profile||state.profile;
+    const result=await api('/api/profile/resume',{method:'POST',body}); onboardingState.resume=result; state.profile=result.profile||state.profile;
     const suggestions=result.analysis?.suggestions||[];
     const found=[...(result.analysis?.detected_skills||result.analysis?.skills||[]),...suggestions.filter(x=>x.field==='skills').map(x=>x.value)].flat().map(v=>String(v||'').trim()).filter(Boolean);
-    onboardingState.selectedSkills=new Set(found); onboardingSetStep(onboardingState.step);
-  }catch(error){toast(error.message)}
+    onboardingState.selectedSkills=new Set(found); onboardingSetStep(onboardingState.step); toast('קורות החיים הועלו ונותחו');
+  }catch(error){label?.classList.remove('uploading');toast(error.message)}
 }
 async function onboardingSaveSkills(){
-  for(const b of $$('[data-ob-skill].selected')){
-    const skill=decodeURIComponent(b.dataset.obSkill||'');
-    if(skill && !(state.profile?.skills||[]).some(v=>v.toLowerCase()===skill.toLowerCase())){
-      const result=await api('/api/profile/skills',{method:'POST',body:JSON.stringify({skill})});
-      if(state.profile)state.profile.skills=result.skills;
-    }
+  for(const skill of onboardingState.selectedSkills){
+    if(skill && !(state.profile?.skills||[]).some(v=>v.toLowerCase()===skill.toLowerCase())){const result=await api('/api/profile/skills',{method:'POST',body:JSON.stringify({skill})});if(state.profile)state.profile.skills=result.skills;}
   }
 }
 function onboardingCollectPreferences(){
-  onboardingState.draft={
-    desired_titles:onboardingSplit($('#ob-titles').value), preferred_locations:onboardingSplit($('#ob-locations').value),
-    years_experience_options:[$('#ob-experience').value], preferred_work_modes:$$('[data-ob-mode].selected').map(b=>b.dataset.obMode),
-    keywords:onboardingSplit($('#ob-keywords').value), excluded_keywords:onboardingSplit($('#ob-excluded').value),
-  }; return onboardingState.draft;
+  onboardingState.draft={desired_titles:onboardingSplit($('#ob-titles').value),preferred_locations:onboardingSplit($('#ob-locations').value),years_experience_options:[$('#ob-experience').value],preferred_work_modes:$$('[data-ob-mode].selected').map(b=>b.dataset.obMode),keywords:onboardingSplit($('#ob-keywords').value),excluded_keywords:onboardingSplit($('#ob-excluded').value)};return onboardingState.draft;
 }
 async function onboardingFinish(skipped=false){
+  if(onboardingState.scanTimer){clearTimeout(onboardingState.scanTimer);onboardingState.scanTimer=null}
   if(!onboardingState.preview)await api('/api/onboarding',{method:'PUT',body:JSON.stringify({completed:!skipped,skipped,step:'done'})});
-  $('#onboarding-gate').hidden=true; $('#onboarding-gate').setAttribute('aria-hidden','true'); document.body.classList.remove('onboarding-open'); onboardingState.preview=false;
+  $('#onboarding-gate').hidden=true;$('#onboarding-gate').setAttribute('aria-hidden','true');document.body.classList.remove('onboarding-open');onboardingState.preview=false;
+}
+async function onboardingWatchScan(){
+  try{
+    const scan=await api('/api/scan/status'), p=scan.progress||{}, total=Number(p.total||0), done=Number(p.completed||0), percent=total?Math.round(done/total*100):0, copy=$('#onboarding-scan-copy');
+    if(copy) copy.textContent=scan.running?`${p.current_source?`סורק עכשיו: ${p.current_source} · `:''}${done}/${total||'…'} מקורות · ${percent}%`:'הסריקה הסתיימה — המשרות שלך מחכות';
+    if(scan.running){onboardingState.scanTimer=setTimeout(onboardingWatchScan,1800);return}
+    const stage=$('.onboarding-scan-stage');stage?.classList.add('complete');const radar=$('.onboarding-radar');if(radar)radar.classList.add('complete');
+    const button=$('#onboarding-start-scan');if(button){button.disabled=false;button.textContent='למשרות שנבחרו עבורך';button.onclick=async()=>{await onboardingFinish();switchView('jobs');await loadJobs()}}
+    const enter=$('#onboarding-enter-site');if(enter)enter.hidden=true;
+  }catch(e){const copy=$('#onboarding-scan-copy');if(copy)copy.textContent='הסריקה ממשיכה ברקע. אפשר להיכנס לאתר ולעקוב משם.';const b=$('#onboarding-start-scan');if(b){b.disabled=false;b.textContent='כניסה ללוח הבקרה';b.onclick=async()=>{await onboardingFinish();switchView('dashboard')}}}
 }
 async function onboardingStartScan(){
-  const b=$('#onboarding-start-scan'); b.disabled=true;b.textContent='מתחיל סריקה…';
-  try{await api('/api/scan',{method:'POST'});await onboardingFinish();switchView('dashboard');pollScan();toast('הסריקה הראשונה התחילה');}catch(e){toast(e.message);b.disabled=false;b.textContent='התחל סריקה ראשונה'}
+  const b=$('#onboarding-start-scan');b.disabled=true;b.textContent='הסריקה יצאה לדרך…';$('.onboarding-scan-stage')?.classList.add('scanning');
+  try{await api('/api/scan',{method:'POST'});const copy=$('#onboarding-scan-copy');if(copy)copy.textContent='מתחבר למקורות ומתחיל לאסוף משרות…';onboardingWatchScan()}catch(e){toast(e.message);b.disabled=false;b.textContent='נסה להתחיל שוב'}
 }
 async function openOnboarding(preview=false){
-  if(authState.user?.is_guest)return;
-  onboardingState.preview=preview;onboardingState.step=0;onboardingState.resume=null;onboardingState.selectedSkills=new Set();onboardingState.draft={};
+  if(authState.user?.is_guest)return;onboardingState.preview=preview;onboardingState.step=0;onboardingState.resume=null;onboardingState.selectedSkills=new Set();onboardingState.draft={};onboardingApplyTrackTheme(state.activeCareerTrack);
   $('#onboarding-gate').hidden=false;$('#onboarding-gate').setAttribute('aria-hidden','false');document.body.classList.add('onboarding-open');onboardingSetStep(0);
 }
-async function maybeOpenOnboarding(){
-  if(authState.user?.is_guest)return;
-  const status=await api('/api/onboarding');
-  if(Number(status.current_version||0)!==ONBOARDING_VERSION) console.warn('Onboarding asset/API version mismatch',status);
-  if(!status.completed) await openOnboarding(false);
-}
+async function maybeOpenOnboarding(){if(authState.user?.is_guest)return;const status=await api('/api/onboarding');if(Number(status.current_version||0)!==ONBOARDING_VERSION)console.warn('Onboarding asset/API version mismatch',status);if(!status.completed)await openOnboarding(false)}
 $('#onboarding-back').onclick=()=>onboardingSetStep(onboardingState.step-1);
 $('#onboarding-skip').onclick=()=>onboardingFinish(true);
-$('#onboarding-next').onclick=async()=>{try{const step=onboardingSteps[onboardingState.step];if(step==='resume')await onboardingSaveSkills();if(step==='preferences'){const d=onboardingCollectPreferences();state.profile=await api('/api/profile',{method:'PATCH',body:JSON.stringify(d)});}onboardingSetStep(onboardingState.step+1)}catch(e){toast(e.message)}};
+$('#onboarding-next').onclick=async()=>{try{const step=onboardingSteps[onboardingState.step];if(step==='skills')await onboardingSaveSkills();if(step==='preferences'){const d=onboardingCollectPreferences();state.profile=await api('/api/profile',{method:'PATCH',body:JSON.stringify(d)});}onboardingSetStep(onboardingState.step+1)}catch(e){toast(e.message)}};
 $('#developer-preview-onboarding').onclick=async()=>{try{await api('/api/admin/onboarding/preview',{method:'POST'});await openOnboarding(true)}catch(e){toast(e.message)}};
+async function loadDeveloperUsers(){
+  const root=$('#developer-users-list'),count=$('#developer-users-count');if(!root)return;
+  root.innerHTML='<div class="empty-state">טוען משתמשים…</div>';
+  try{const payload=await api('/api/admin/users');count.textContent=String(payload.count||0);root.innerHTML=(payload.users||[]).map(u=>`<article class="developer-user-row"><span class="cloud-user-avatar">${esc((u.email||'?').slice(0,1).toUpperCase())}</span><span><strong>${esc(u.email||u.id)}</strong><small>${u.role==='admin'?'Admin':'משתמש'} · ${u.last_seen_at?`נראה לאחרונה ${esc(new Date(u.last_seen_at).toLocaleString('he-IL'))}`:'טרם התחבר'}</small></span></article>`).join('')||'<div class="empty-state">אין משתמשים להצגה</div>'}catch(e){root.innerHTML=`<div class="empty-state">${esc(e.message)}</div>`}
+}
+$('#developer-refresh-users').onclick=loadDeveloperUsers;
 function configureDeveloperTools(){
-  const allowed = authState.config?.mode!=='supabase' || authState.capabilities?.developer_tools === true;
-  $$('.admin-only-nav').forEach(el=>el.hidden=!allowed);
-  const status=$('#developer-runtime-status');
-  if(status) status.textContent=allowed
-    ? `מחובר כ-${authState.user?.email||'local'} · role: ${authState.user?.role||'admin'} · onboarding v${ONBOARDING_VERSION}`
-    : '';
+  const allowed=authState.config?.mode!=='supabase'||authState.capabilities?.developer_tools === true;$$('.admin-only-nav').forEach(el=>el.hidden=!allowed);const status=$('#developer-runtime-status');if(status)status.textContent=allowed?`מחובר כ־${authState.user?.email||'local'} · role: ${authState.user?.role||'admin'} · onboarding v${ONBOARDING_VERSION}`:'';if(allowed)loadDeveloperUsers();
 }
 
 (async () => {
diff --git a/app/static/index.html b/app/static/index.html
index e55abbe..6557b52 100644
--- a/app/static/index.html
+++ b/app/static/index.html
@@ -5,7 +5,7 @@
   <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
   <title>JobPilot — חיפוש והגשת משרות חכמה</title>
   <link rel="icon" type="image/svg+xml" href="/static/jobpilot-mark.svg?v=17" />
-  <link rel="stylesheet" href="/static/styles.css?v=0.46.0" />
+  <link rel="stylesheet" href="/static/styles.css?v=0.47.0" />
 </head>
 <body>
   <section id="auth-gate" class="auth-gate" hidden aria-live="polite">
@@ -236,7 +236,9 @@
       <section id="view-developer" class="view">
         <article class="panel developer-panel">
           <div class="panel-head"><div><span class="kicker">Admin only</span><h2>אפשרויות למפתחים</h2><p class="preference-help">כלי בדיקה פנימיים שרק האדמין רואה.</p></div></div>
-          <p id="developer-runtime-status" class="preference-help"></p>\n          <button class="btn primary" id="developer-preview-onboarding" type="button">פתח את מסך הפתיחה לבדיקה</button>
+          <p id="developer-runtime-status" class="preference-help"></p>
+          <div class="developer-actions"><button class="btn primary" id="developer-preview-onboarding" type="button">פתח את מסך הפתיחה לבדיקה</button><button class="btn secondary" id="developer-refresh-users" type="button">רענן משתמשים</button></div>
+          <section class="developer-users-card"><div class="panel-head"><div><span class="kicker">משתמשי המערכת</span><h3>חשבונות שנכנסו ל־JobPilot</h3></div><strong id="developer-users-count">—</strong></div><div id="developer-users-list" class="developer-users-list"><div class="empty-state">טוען משתמשים…</div></div></section>
         </article>
       </section>
 
@@ -427,7 +429,7 @@
 
   <section id="onboarding-gate" class="onboarding-gate" hidden aria-hidden="true">
     <div class="onboarding-shell" role="dialog" aria-modal="true" aria-labelledby="onboarding-title">
-      <header class="onboarding-head"><div class="onboarding-brand"><strong>JP</strong><span>JobPilot</span></div><span id="onboarding-progress-label">1 מתוך 5</span></header>
+      <header class="onboarding-head"><div class="onboarding-brand brand" aria-label="JobPilot"><span class="brand-mark onboarding-brand-mark"><svg viewBox="0 0 64 64" aria-hidden="true"><defs><linearGradient id="ob-jp-surface" x1="8" y1="5" x2="56" y2="60" gradientUnits="userSpaceOnUse"><stop stop-color="#123F65"/><stop offset=".55" stop-color="#0B3151"/><stop offset="1" stop-color="#06243E"/></linearGradient><linearGradient id="ob-jp-line" x1="18" y1="49" x2="50" y2="16" gradientUnits="userSpaceOnUse"><stop stop-color="#8FD3F4"/><stop offset=".48" stop-color="#DDF5FF"/><stop offset="1" stop-color="#FFFFFF"/></linearGradient></defs><rect class="logo-surface" x="3" y="3" width="58" height="58" rx="18" fill="url(#ob-jp-surface)"/><path class="logo-route-depth" d="M27 22C26 31 24 42 19 49C15 55 9 52 10 46C11 39 21 34 33 27C43 20 51 20 55 26C59 33 55 42 48 44C43 45 39 43 36 40C37 36 38 31 39 27C37 35 34 47 31 54"/><path class="logo-route" pathLength="100" d="M27 22C26 31 24 42 19 49C15 55 9 52 10 46C11 39 21 34 33 27C43 20 51 20 55 26C59 33 55 42 48 44C43 45 39 43 36 40C37 36 38 31 39 27C37 35 34 47 31 54"/><path class="logo-route-highlight" pathLength="100" d="M27 22C26 31 24 42 19 49C15 55 9 52 10 46C11 39 21 34 33 27C43 20 51 20 55 26C59 33 55 42 48 44C43 45 39 43 36 40C37 36 38 31 39 27C37 35 34 47 31 54"/></svg></span><i class="brand-flight-dot" aria-hidden="true"></i><span class="onboarding-wordmark">JobPilot</span></div><span id="onboarding-progress-label">1 מתוך 6</span></header>
       <div class="onboarding-progress"><i id="onboarding-progress-bar"></i></div>
       <div id="onboarding-content" class="onboarding-content"></div>
       <footer class="onboarding-actions"><button id="onboarding-back" class="btn secondary" type="button">חזרה</button><button id="onboarding-skip" class="text-btn" type="button">אעשה את זה אחר כך</button><button id="onboarding-next" class="btn primary" type="button">המשך</button></footer>
@@ -438,6 +440,6 @@
   <aside id="notification-center" class="notification-center" aria-hidden="true" aria-label="מרכז התראות"><div class="notification-head"><div><span class="kicker">עדכונים ופעולות</span><h2>מרכז התראות</h2></div><button type="button" id="notification-close" aria-label="סגור התראות">×</button></div><div id="notification-list" class="notification-list"></div></aside>
   <div id="command-palette" class="command-palette" aria-hidden="true"><div class="command-card" role="dialog" aria-modal="true" aria-label="חיפוש מהיר"><label><span>⌕</span><input id="command-input" type="search" placeholder="חיפוש משרה, חברה, סקיל או מעבר לעמוד…" autocomplete="off" /></label><div id="command-results" class="command-results"></div><small>Enter לבחירה · Esc לסגירה · ⌘K לפתיחה</small></div></div>
   <div id="toast" class="toast" role="status" aria-live="polite"></div>
-  <script src="/static/app.js?v=0.27.1"></script>
+  <script src="/static/app.js?v=0.28.0"></script>
 </body>
 </html>
diff --git a/app/static/styles.css b/app/static/styles.css
index 6b8c531..21c972d 100644
--- a/app/static/styles.css
+++ b/app/static/styles.css
@@ -2833,3 +2833,16 @@ body.guest-mode .job-status-control { display:none !important; }
 .onboarding-actions{display:flex;align-items:center;gap:10px;border-top:1px solid var(--line);padding-top:18px}.onboarding-actions #onboarding-next{margin-inline-start:auto}.developer-panel{max-width:760px}
 @media(max-width:700px){.onboarding-gate{padding:0;place-items:end center}.onboarding-shell{width:100%;max-height:94dvh;border-radius:24px 24px 0 0;padding:20px 16px calc(18px + env(safe-area-inset-bottom));box-shadow:0 -18px 55px rgba(0,0,0,.2)}.onboarding-content{min-height:360px}.onboarding-hero{padding:28px 0 12px}.onboarding-form,.onboarding-summary{grid-template-columns:1fr}.onboarding-form>*{grid-column:1!important}.onboarding-actions{position:sticky;bottom:0;background:var(--panel);padding-bottom:2px}}
 @media(prefers-reduced-motion:reduce){.onboarding-shell{animation:none}.onboarding-progress i{transition:none}}
+
+/* Onboarding v3 — career-aware, visual upload state, polished review and scan */
+.onboarding-brand.brand{position:relative;display:flex;align-items:center;gap:10px;min-height:48px}.onboarding-brand .onboarding-brand-mark{width:48px;height:48px;flex:0 0 48px}.onboarding-brand .onboarding-wordmark{font-size:22px;font-weight:900;letter-spacing:-.04em}.onboarding-brand .brand-flight-dot{display:block}
+.onboarding-hero.compact{padding:12px 0 8px}.onboarding-track-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px;margin-top:26px}.onboarding-track-card{display:grid;grid-template-columns:auto 1fr auto;align-items:center;gap:14px;text-align:start;border:1px solid var(--line);background:var(--panel-2);color:var(--text);padding:18px;border-radius:20px;cursor:pointer;transition:.2s ease}.onboarding-track-card:hover,.onboarding-track-card.active{border-color:var(--brand);box-shadow:0 10px 30px var(--accent-glow);transform:translateY(-2px)}.onboarding-track-symbol{display:grid;place-items:center;width:52px;height:52px;border-radius:16px;background:var(--brand);color:#fff;font-weight:900}.onboarding-track-card span:nth-child(2){display:grid;gap:4px}.onboarding-track-card small{color:var(--muted);line-height:1.45}.onboarding-track-card i{font-style:normal;color:var(--brand);font-size:12px;font-weight:800}
+.onboarding-upload{position:relative;overflow:hidden;transition:.22s ease}.onboarding-upload.uploading{opacity:.72;pointer-events:none}.onboarding-upload.uploaded{border-style:solid;border-color:var(--brand);background:linear-gradient(135deg,var(--brand-soft),var(--panel-2));box-shadow:inset 0 0 0 1px var(--accent-glow),0 14px 34px var(--accent-glow)}.onboarding-upload-icon{display:grid;place-items:center;width:46px;height:46px;border-radius:15px;background:var(--brand);color:#fff;font-size:24px;font-weight:900;margin-bottom:10px}.onboarding-upload em{margin-top:10px;padding:5px 10px;border-radius:999px;background:var(--brand);color:#fff;font-size:11px;font-style:normal;font-weight:800}
+.onboarding-skill-checks{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:22px;max-height:310px;overflow:auto;padding:3px}.onboarding-skill-check{display:flex;align-items:center;gap:10px;min-height:52px;padding:11px 12px;border:1px solid var(--line);border-radius:15px;background:var(--panel-2);cursor:pointer;transition:.18s ease}.onboarding-skill-check input{position:absolute;opacity:0}.onboarding-skill-check .checkmark{display:grid;place-items:center;width:23px;height:23px;border-radius:7px;border:1px solid var(--line);color:transparent;background:var(--panel)}.onboarding-skill-check.selected{border-color:var(--brand);background:var(--brand-soft);color:var(--brand)}.onboarding-skill-check.selected .checkmark{background:var(--brand);border-color:var(--brand);color:#fff}.onboarding-skill-check strong{font-size:13px}
+.onboarding-form.polished{gap:16px}.onboarding-form.polished label,.onboarding-form.polished fieldset{background:var(--panel-2);border:1px solid var(--line);border-radius:17px;padding:13px}.onboarding-form.polished input,.onboarding-form.polished select{background:var(--panel);border-color:transparent;box-shadow:0 0 0 1px var(--line)}
+.onboarding-review-hero{display:flex;align-items:center;gap:12px;margin:20px 0 12px;padding:14px 16px;border-radius:18px;background:linear-gradient(135deg,var(--brand-soft),var(--panel-2));border:1px solid var(--accent-border)}.onboarding-review-hero>div{display:grid;gap:3px}.onboarding-review-hero small{color:var(--muted)}.onboarding-summary.refined article{min-height:94px;align-content:center;border-radius:18px;background:linear-gradient(145deg,var(--panel-2),var(--panel))}
+.onboarding-scan-stage{text-align:center;padding:12px 0}.onboarding-scan-stage>p{margin-inline:auto}.onboarding-radar{position:relative;width:190px;height:190px;margin:22px auto;border-radius:50%;border:1px solid var(--accent-border);background:radial-gradient(circle,var(--brand-soft) 0 5%,transparent 6% 32%,var(--accent-border) 33% 34%,transparent 35% 63%,var(--accent-border) 64% 65%,transparent 66%);overflow:hidden}.onboarding-radar:before{content:"";position:absolute;inset:50% 50% 0 0;transform-origin:100% 0;background:linear-gradient(25deg,transparent,var(--accent-glow-strong));animation:onboarding-radar-spin 1.7s linear infinite}.onboarding-radar i{position:absolute;width:9px;height:9px;border-radius:50%;background:var(--brand);box-shadow:0 0 0 5px var(--accent-glow)}.onboarding-radar i:nth-child(1){top:35px;right:58px}.onboarding-radar i:nth-child(2){bottom:45px;left:42px}.onboarding-radar i:nth-child(3){top:88px;left:30px}.onboarding-radar b{position:absolute;inset:50%;width:12px;height:12px;transform:translate(-50%,-50%);border-radius:50%;background:var(--brand)}.onboarding-radar.complete:after{content:"✓";position:absolute;inset:0;display:grid;place-items:center;background:color-mix(in srgb,var(--panel) 88%,transparent);color:var(--brand);font-size:68px;font-weight:900}.onboarding-scan-copy{min-height:26px;color:var(--muted);font-weight:700;margin-bottom:12px}.onboarding-scan-stage.complete .onboarding-scan{animation:onboarding-cta-pop .45s ease both}@keyframes onboarding-radar-spin{to{transform:rotate(360deg)}}@keyframes onboarding-cta-pop{0%{transform:scale(.92);opacity:.3}70%{transform:scale(1.04)}100%{transform:none;opacity:1}}
+.developer-actions{display:flex;gap:10px;flex-wrap:wrap}.developer-users-card{margin-top:20px;padding:16px;border:1px solid var(--line);border-radius:20px;background:var(--panel-2)}.developer-users-list{display:grid;gap:8px;max-height:292px;overflow-y:auto;padding-inline-end:4px}.developer-user-row{display:flex;align-items:center;gap:11px;min-height:64px;padding:10px 12px;border:1px solid var(--line);border-radius:14px;background:var(--panel)}.developer-user-row>span:last-child{display:grid;gap:3px;min-width:0}.developer-user-row strong{overflow:hidden;text-overflow:ellipsis}.developer-user-row small{color:var(--muted)}
+.resume-profile-card.resume-uploaded{border-color:var(--brand);background:var(--brand-soft);box-shadow:0 8px 24px var(--accent-glow)}
+@media(max-width:700px){.onboarding-track-grid{grid-template-columns:1fr}.onboarding-skill-checks{grid-template-columns:repeat(2,minmax(0,1fr));max-height:280px}.onboarding-track-card{padding:14px}.onboarding-review-hero{margin-top:12px}.onboarding-radar{width:150px;height:150px}.developer-users-list{max-height:272px}}
+@media(prefers-reduced-motion:reduce){.onboarding-radar:before,.onboarding-scan-stage.complete .onboarding-scan{animation:none!important}}
diff --git a/tests/test_api.py b/tests/test_api.py
index 2d64412..18fc561 100644
--- a/tests/test_api.py
+++ b/tests/test_api.py
@@ -259,7 +259,7 @@ def test_frontend_assets_are_never_stale_after_an_update():
         assert "no-store" in index.headers["cache-control"]
         assert "no-store" in script.headers["cache-control"]
         assert "no-store" in stylesheet.headers["cache-control"]
-        assert "app.js?v=0.27.1" in index.text
+        assert "app.js?v=0.28.0" in index.text
         assert "הנתון לא נשמר עדיין" in script.text
 
 
diff --git a/tests/test_career_track_browser_offline.py b/tests/test_career_track_browser_offline.py
index 5072747..45fcb37 100644
--- a/tests/test_career_track_browser_offline.py
+++ b/tests/test_career_track_browser_offline.py
@@ -26,8 +26,8 @@ def test_real_browser_switches_profession_theme_options_and_agent_state():
     html = (ROOT / "app" / "static" / "index.html").read_text()
     css = (ROOT / "app" / "static" / "styles.css").read_text()
     js = (ROOT / "app" / "static" / "app.js").read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.46.0" />', f"<style>{css}</style>")
-    html = html.replace('<script src="/static/app.js?v=0.27.1"></script>', "")
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.47.0" />', f"<style>{css}</style>")
+    html = html.replace('<script src="/static/app.js?v=0.28.0"></script>', "")
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=["--no-sandbox"])
diff --git a/tests/test_cloud_ui_offline.py b/tests/test_cloud_ui_offline.py
index 41f4081..5ef8068 100644
--- a/tests/test_cloud_ui_offline.py
+++ b/tests/test_cloud_ui_offline.py
@@ -28,8 +28,8 @@ def test_cloud_session_hides_login_and_shows_account_and_agent_state():
     html = (ROOT / "app" / "static" / "index.html").read_text()
     css = (ROOT / "app" / "static" / "styles.css").read_text()
     js = (ROOT / "app" / "static" / "app.js").read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.46.0" />', f"<style>{css}</style>")
-    html = html.replace('<script src="/static/app.js?v=0.27.1"></script>', "")
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.47.0" />', f"<style>{css}</style>")
+    html = html.replace('<script src="/static/app.js?v=0.28.0"></script>', "")
     session = {"access_token": _fake_jwt(), "refresh_token": "refresh", "token_type": "bearer"}
 
     with sync_playwright() as playwright:
@@ -85,7 +85,7 @@ def test_cloud_without_session_shows_login_gate():
     chromium = _chromium_path()
     if not chromium:
         pytest.skip("No system Chromium executable")
-    html = (ROOT / "app" / "static" / "index.html").read_text().replace('<script src="/static/app.js?v=0.27.1"></script>', "")
+    html = (ROOT / "app" / "static" / "index.html").read_text().replace('<script src="/static/app.js?v=0.28.0"></script>', "")
     js = (ROOT / "app" / "static" / "app.js").read_text()
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=["--no-sandbox"])
@@ -118,8 +118,8 @@ def test_cloud_non_owner_cannot_pair_or_enable_application_agent():
     html = (ROOT / "app" / "static" / "index.html").read_text()
     css = (ROOT / "app" / "static" / "styles.css").read_text()
     js = (ROOT / "app" / "static" / "app.js").read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.46.0" />', f"<style>{css}</style>")
-    html = html.replace('<script src="/static/app.js?v=0.27.1"></script>', "")
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.47.0" />', f"<style>{css}</style>")
+    html = html.replace('<script src="/static/app.js?v=0.28.0"></script>', "")
     session = {"access_token": _fake_jwt(), "refresh_token": "refresh", "token_type": "bearer"}
 
     with sync_playwright() as playwright:
diff --git a/tests/test_dashboard_metrics_compact.py b/tests/test_dashboard_metrics_compact.py
index 095046d..07e3704 100644
--- a/tests/test_dashboard_metrics_compact.py
+++ b/tests/test_dashboard_metrics_compact.py
@@ -26,5 +26,5 @@ def test_metrics_stay_compact_on_mobile_and_support_dark_mode():
 
 
 def test_v0111_asset_versions_are_bumped():
-    assert 'styles.css?v=0.46.0' in HTML
-    assert 'app.js?v=0.27.1' in HTML
+    assert 'styles.css?v=0.47.0' in HTML
+    assert 'app.js?v=0.28.0' in HTML
diff --git a/tests/test_google_oauth_contract.py b/tests/test_google_oauth_contract.py
index 89754f3..5804c7f 100644
--- a/tests/test_google_oauth_contract.py
+++ b/tests/test_google_oauth_contract.py
@@ -19,4 +19,4 @@ def test_google_oauth_callback_surfaces_provider_errors_from_hash_or_query():
 
 
 def test_google_oauth_asset_version_is_bumped():
-    assert 'app.js?v=0.27.1' in HTML
+    assert 'app.js?v=0.28.0' in HTML
diff --git a/tests/test_mobile_logo_contract.py b/tests/test_mobile_logo_contract.py
index f146bbb..e0ac4bc 100644
--- a/tests/test_mobile_logo_contract.py
+++ b/tests/test_mobile_logo_contract.py
@@ -6,7 +6,7 @@ HTML = (ROOT / "app" / "static" / "index.html").read_text()
 
 
 def test_mobile_header_keeps_full_animated_jobpilot_brand():
-    assert 'styles.css?v=0.46.0' in HTML
+    assert 'styles.css?v=0.47.0' in HTML
     assert '.sidebar .brand > div { display:block !important;' in CSS
     assert '.sidebar .brand-flight-dot { display:block !important;' in CSS
     assert '.sidebar .brand strong { display:block;' in CSS
diff --git a/tests/test_mobile_skill_performance_contract.py b/tests/test_mobile_skill_performance_contract.py
index 5bc3e07..b2428fb 100644
--- a/tests/test_mobile_skill_performance_contract.py
+++ b/tests/test_mobile_skill_performance_contract.py
@@ -45,5 +45,5 @@ def test_iem_uses_same_generic_tab_copy_as_cs():
 
 
 def test_asset_versions_are_bumped_for_mobile_and_skill_fix():
-    assert 'styles.css?v=0.46.0' in HTML
-    assert 'app.js?v=0.27.1' in HTML
+    assert 'styles.css?v=0.47.0' in HTML
+    assert 'app.js?v=0.28.0' in HTML
diff --git a/tests/test_onboarding_runtime_contract.py b/tests/test_onboarding_runtime_contract.py
index d1a3d2f..5c62079 100644
--- a/tests/test_onboarding_runtime_contract.py
+++ b/tests/test_onboarding_runtime_contract.py
@@ -9,7 +9,7 @@ HTML = (ROOT / "app/static/index.html").read_text()
 def test_corrected_onboarding_generation_forces_one_clean_rerun():
     assert main.ONBOARDING_VERSION == 2
     assert "const ONBOARDING_VERSION = 2" in JS
-    assert "app.js?v=0.27.1" in HTML
+    assert "app.js?v=0.28.0" in HTML
 
 def test_developer_tools_are_server_authorized(monkeypatch):
     monkeypatch.setattr(main.settings, "auth_mode", "supabase")
diff --git a/tests/test_onboarding_v1.py b/tests/test_onboarding_v1.py
index 05fcd67..058d9c7 100644
--- a/tests/test_onboarding_v1.py
+++ b/tests/test_onboarding_v1.py
@@ -10,8 +10,8 @@ def test_onboarding_assets_and_admin_preview_exist():
     assert 'id="onboarding-gate"' in HTML
     assert 'data-view="developer"' in HTML
     assert 'id="developer-preview-onboarding"' in HTML
-    assert 'app.js?v=0.27.1' in HTML
-    assert 'styles.css?v=0.46.0' in HTML
+    assert 'app.js?v=0.28.0' in HTML
+    assert 'styles.css?v=0.47.0' in HTML
     assert "const ONBOARDING_VERSION = 2" in JS
     assert "maybeOpenOnboarding" in JS
     assert ".onboarding-gate" in CSS
diff --git a/jp_v3_work/tests/test_onboarding_v3_contract.py b/tests/test_onboarding_v3_contract.py
new file mode 100644
index 0000000..276982b
--- /dev/null
+++ b/tests/test_onboarding_v3_contract.py
@@ -0,0 +1,43 @@
+from pathlib import Path
+
+ROOT=Path(__file__).resolve().parents[1]
+HTML=(ROOT/'app/static/index.html').read_text()
+JS=(ROOT/'app/static/app.js').read_text()
+CSS=(ROOT/'app/static/styles.css').read_text()
+
+def test_onboarding_starts_with_extensible_career_track_choice():
+    assert "onboardingSteps = ['track','resume','skills','preferences','review','scan']" in JS
+    assert 'state.careerTracks' in JS
+    assert 'onboardingTrackConfig' in JS
+    assert 'onboardingChooseTrack' in JS
+
+def test_resume_upload_has_visual_success_and_pdf_docx_support():
+    assert 'accept=".pdf,.docx,.doc,.txt,.rtf"' in JS
+    assert "onboarding-upload ${uploaded?'uploaded':''}" in JS
+    assert '.onboarding-upload.uploaded' in CSS
+
+def test_skills_are_checkbox_cards_and_career_aware():
+    assert 'onboarding-skill-check' in JS
+    assert 'type="checkbox" data-ob-skill' in JS
+    assert 'onboardingPresetSkills' in JS
+
+def test_real_animated_logo_is_used_in_onboarding():
+    assert 'onboarding-brand-mark' in HTML
+    assert 'logo-route-highlight' in HTML
+    assert 'brand-flight-dot' in HTML
+    assert '<strong>JP</strong><span>JobPilot</span>' not in HTML
+
+def test_scan_has_animation_and_post_scan_jobs_cta():
+    assert 'onboarding-radar' in JS
+    assert 'onboardingWatchScan' in JS
+    assert 'למשרות שנבחרו עבורך' in JS
+
+def test_developer_users_panel_is_scroll_limited():
+    assert 'id="developer-users-list"' in HTML
+    assert "api('/api/admin/users')" in JS
+    assert '.developer-users-list' in CSS
+    assert 'max-height:292px' in CSS
+
+def test_assets_bumped():
+    assert 'app.js?v=0.28.0' in HTML
+    assert 'styles.css?v=0.47.0' in HTML
diff --git a/tests/test_scoped_save_browser_offline.py b/tests/test_scoped_save_browser_offline.py
index 79437e7..1c9731d 100644
--- a/tests/test_scoped_save_browser_offline.py
+++ b/tests/test_scoped_save_browser_offline.py
@@ -20,8 +20,8 @@ def test_each_profile_card_saves_only_its_dirty_fields_and_keeps_other_drafts():
     html = (ROOT / 'app' / 'static' / 'index.html').read_text()
     css = (ROOT / 'app' / 'static' / 'styles.css').read_text()
     js = (ROOT / 'app' / 'static' / 'app.js').read_text()
-    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.46.0" />', f'<style>{css}</style>')
-    html = html.replace('<script src="/static/app.js?v=0.27.1"></script>', '')
+    html = html.replace('<link rel="stylesheet" href="/static/styles.css?v=0.47.0" />', f'<style>{css}</style>')
+    html = html.replace('<script src="/static/app.js?v=0.28.0"></script>', '')
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=['--no-sandbox'])
diff --git a/tests/test_ui_offline.py b/tests/test_ui_offline.py
index e8037d8..8c78ae0 100644
--- a/tests/test_ui_offline.py
+++ b/tests/test_ui_offline.py
@@ -19,7 +19,7 @@ def test_delete_buttons_and_israel_scan_report_in_real_browser_without_server():
         pytest.skip("No system Chromium executable")
 
     html = (PROJECT_ROOT / "app" / "static" / "index.html").read_text()
-    html = html.replace('<script src="/static/app.js?v=0.27.1"></script>', "")
+    html = html.replace('<script src="/static/app.js?v=0.28.0"></script>', "")
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(
@@ -108,7 +108,7 @@ def test_compact_blockers_and_handoff_links_render_inside_application_queue():
         pytest.skip("No system Chromium executable")
 
     html = (PROJECT_ROOT / "app" / "static" / "index.html").read_text()
-    html = html.replace('<script src="/static/app.js?v=0.27.1"></script>', "")
+    html = html.replace('<script src="/static/app.js?v=0.28.0"></script>', "")
 
     with sync_playwright() as playwright:
         browser = playwright.chromium.launch(headless=True, executable_path=chromium, args=["--no-sandbox"])
diff --git a/tests/test_ui_polish_contract.py b/tests/test_ui_polish_contract.py
index 1c975c9..c996b20 100644
--- a/tests/test_ui_polish_contract.py
+++ b/tests/test_ui_polish_contract.py
@@ -93,5 +93,5 @@ def test_mobile_redesign_uses_simple_fixed_bottom_dock_and_phone_first_job_layou
     assert "$$('[data-mobile-view]')" in JS
     assert 'scrollIntoView' in JS
     assert "function jobCardActions(job)" in JS
-    assert 'app.js?v=0.27.1' in HTML
-    assert 'styles.css?v=0.46.0' in HTML
+    assert 'app.js?v=0.28.0' in HTML
+    assert 'styles.css?v=0.47.0' in HTML
